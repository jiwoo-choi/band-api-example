import { RemoteContent } from "nextra/data";
import { buildDynamicMDX, buildDynamicMeta } from 'nextra/remote'
import { Tabs, Cards, Card} from 'nextra/components'
import { fullCodePartCodeGen } from '../../code';

export const getStaticProps = async ({ params }) => {
   return { 
    props: {
        ...(await buildDynamicMeta()),
        ...(await buildDynamicMDX(
          await fullCodePartCodeGen({
            fileName: 'write_post', 
            headerTitle: '글 작성',
            description: ''
          })
         , {defaultShowCopyCode: true, codeHighlight: true}))
    }
  }
};

<RemoteContent components={{Tabs, TabItem: Tabs.Tab, Cards, Card }}/>